services:
  tex-env:
    build: .
    volumes:
      # docker-compose.yml が .devcontainer 内にあるため、
      # 親ディレクトリ（プロジェクトルート）を /workdir にマウントする
      - ..:/workdir
    # ホストとコンテナのファイル権限を同期させるための設定
    # USER_ID=${UID} を .env で指定可能
    user: "${USER_ID:-root}"
    tty: true
    stdin_open: true
